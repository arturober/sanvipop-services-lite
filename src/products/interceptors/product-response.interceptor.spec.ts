import { ProductResponseInterceptor } from './product-response.interceptor';

describe('ProductResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new ProductResponseInterceptor()).toBeDefined();
  });
});
