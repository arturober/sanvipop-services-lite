import { PhotoResponseInterceptor } from './photo-response.interceptor';

describe('PhotoResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new PhotoResponseInterceptor()).toBeDefined();
  });
});
