import { ProductListResponseInterceptor } from './product-list-response.interceptor';

describe('ProductListResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new ProductListResponseInterceptor()).toBeDefined();
  });
});
